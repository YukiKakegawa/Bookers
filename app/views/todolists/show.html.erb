<h2>Title</h2>
<p><%= @book.title %></p>
<h2>Body</h2>
<p><%= @book.body %></p>

<%= link_to "Show", show_todolist_path(@book.id) %>
<%= link_to "Edit", edit_todolist_path(@book.id) %>
<%= link_to "Destroy", destroy_todolist_path(@book.id) %>


